use crate::controller::AppController;use notepad_core::{LineColour,Result};use std::sync::Arc;#[derive(Clone)]pub struct CallbackDispatcher{pub controller:Arc<AppController>}impl CallbackDispatcher{pub fn new(c:Arc<AppController>)->Self{Self{controller:c}}pub fn highlight(&self,c:LineColour){self.controller.apply_highlight(c)}pub fn save(&self)->Result<bool>{self.controller.save_active()}pub fn close(&self)->bool{self.controller.close_window()}}
